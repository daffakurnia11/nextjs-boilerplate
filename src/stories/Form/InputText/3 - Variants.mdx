import { Canvas, Meta, Source } from "@storybook/blocks";
import * as InputStories from "./Input.stories";

<Meta of={InputStories} />

# Input Text

## Variants

The `Input.Text` component has some variants that can be modified by own. This component has some properties for providing the design and the requirements of input component inside the application. Below are the example of some customization of the `Input.Text` component.

### Basic Input

This section showcases the basic component of `Input.Text` that can be used inside the application. The properties that must be used are `name` prop for determining the name of the input component and `type` prop for determining the type of the input that can be `text` or `email`.

<Canvas of={InputStories.Basic} />
<Source
  code={`
<Input.Text name="name" type="text" />
`}
/>

### Adding Label

This section showcases the `Input.Text` component when the component needs to provide the label on the top of the component. Provide the `label` prop for giving the label of the input component.

<Canvas of={InputStories.AddingLabel} />
<Source
  code={`
<Input.Text name="name" type="text" label="Label" />
`}
/>

### Adding Label and Required

This section showcases the `Input.Text` component when the component needs to provide the label and required mark inside the component. Provide the `label` prop for giving the label of the input component and also `required` to be `true` for adding the `*` mark after the label.

<Canvas of={InputStories.AddingRequired} />
<Source
  code={`
<Input.Text name="name" type="text" label="Label" required />
`}
/>

### Adding Placeholder

This section showcases the `Input.Text` component when the component needs to provide the placeholder inside the component. Provide the `placeholder` prop for giving the placeholder of the input component.

<Canvas of={InputStories.AddingPlaceholder} />
<Source
  code={`
<Input.Text name="name" type="text" placeholder="Placeholder" />
`}
/>

### Disabled Input

This section showcases the `Input.Text` component when the component needs to be disabled inside the form component. Provide the `disabled` prop for determining the input is disabled.

<Canvas of={InputStories.DisabledInput} />
<Source
  code={`
<Input.Text name="name" type="text" disabled />
`}
/>

### Read-only Input

This section showcases the `Input.Text` component when the component needs to be read-only inside the form component. Provide the `readonly` prop for determining the input is read-only.

<Canvas of={InputStories.ReadonlyInput} />
<Source
  code={`
<Input.Text name="name" type="text" readonly />
`}
/>

### Adding Show Counts

This section showcases the `Input.Text` component when the component needs to provide the show counts on the top of the component. Provide the `showCount` prop for giving the show counts of the input component and also `maxLength` prop for determining the maximum value of input component.

<Canvas of={InputStories.AddingShowCount} />
<Source
  code={`
<Input.Text name="name" type="text" showCount maxLength={20} />
`}
/>

### Adding Helper Text

This section showcases the `Input.Text` component when the component needs to provide the helper text on the bottom of the component. Provide the `helperText` prop for giving the helper text of the input component.

<Canvas of={InputStories.AddingHelperText} />
<Source
  code={`
<Input.Text name="name" type="text" helperText="Helper Text" />
`}
/>

### Adding Prefix Icon

This section showcases the `Input.Text` component when the component needs to provide the prefix icon inside the component that places on the left of the input text. Provide the `prefix` prop for giving the prefix icon of the input component. This example uses the **React Icons** library for generating the icon.

<Canvas of={InputStories.AddingPrefix} />
<Source
  code={`
<Input.Text name="name" type="text" prefix={<AiOutlineUser size={20} />} />
`}
/>

### Adding Suffix Icon

This section showcases the `Input.Text` component when the component needs to provide the suffix icon inside the component that places on the right of the input text. Provide the `suffix` prop for giving the suffix icon of the input component. This example uses the **React Icons** library for generating the icon.

<Canvas of={InputStories.AddingSuffix} />
<Source
  code={`
<Input.Text name="name" type="text" suffix={<AiOutlineUser size={20} />} />
`}
/>
