import { Canvas, Controls, Meta, Source, Stories } from "@storybook/blocks";

import * as ComponentStories from "./Docs.stories";

<Meta of={ComponentStories} />

# Tertiary Button

## Icon-only Variants

Type `Tertiary` button also supports the icon-only type of the using `iconOnly` property that sets to be `true`. This component has a different padding style and only supports for the icon label. The `size` and the `type` properties can also be added to ensure it fits for the design requirements.

### Default Button

This section showcases an example of the `Tertiary` button component in `iconOnly` state at `default` button type that has a background color and border color that using `.bg-tertiary-700` and `.border-tertiary-700` class from **TailwindCSS**, as specified in `tailwind.config.ts`.

<Canvas of={ComponentStories.IconOnlyDefault} />

<Source
  code={`
<Button.Tertiary iconOnly={true} size="lg" type="default">
  <AiOutlinePlus />
</Button.Tertiary>
`}
/>

### Outline Button

This section showcases an example of the `Tertiary` button component in `iconOnly` state at `outline` button type that has a transparent background color and border color that using `.bg-transparent` and `.border-tertiary-700` class from **TailwindCSS**, as specified in `tailwind.config.ts`.

<Canvas of={ComponentStories.IconOnlyOutline} />

<Source
  code={`
<Button.Tertiary iconOnly={true} size="lg" type="outline">
  <AiOutlinePlus />
</Button.Tertiary>
`}
/>

### Text-only Button

This section showcases an example of the `Tertiary` button component in `iconOnly` state at `text` button type that has a transparent background color and transparent border color that using `.bg-transparent` and `.border-transparent` class from **TailwindCSS**, as specified in `tailwind.config.ts`.

<Canvas of={ComponentStories.IconOnlyTextOnly} />

<Source
  code={`
<Button.Tertiary iconOnly={true} size="lg" type="text">
  <AiOutlinePlus />
</Button.Tertiary>
`}
/>
