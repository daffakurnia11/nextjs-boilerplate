import { Canvas, Controls, Meta, Source, Stories } from "@storybook/blocks";

import * as ComponentStories from "./Docs.stories";

<Meta of={ComponentStories} />

# Primary Button

## Icon-only Variants

Type `Primary` button also supports the icon-only type of the using `iconOnly` property that sets to be `true`. This component has a different padding style and only supports for the icon label. The `size` and the `type` properties can also be added to ensure it fits for the design requirements.

### Default Button

This section showcases an example of the `Primary` button component in `iconOnly` state at `default` button type that has a background color and border color that using `.bg-primary-700` and `.border-primary-700` class from **TailwindCSS**, as specified in `tailwind.config.ts`.

<Canvas of={ComponentStories.IconOnlyDefault} />

<Source
  code={`
<Button.Primary iconOnly={true} size="lg" type="default">
  <AiOutlinePlus />
</Button.Primary>
`}
/>

### Outline Button

This section showcases an example of the `Primary` button component in `iconOnly` state at `outline` button type that has a transparent background color and border color that using `.bg-transparent` and `.border-primary-700` class from **TailwindCSS**, as specified in `tailwind.config.ts`.

<Canvas of={ComponentStories.IconOnlyOutline} />

<Source
  code={`
<Button.Primary iconOnly={true} size="lg" type="outline">
  <AiOutlinePlus />
</Button.Primary>
`}
/>

### Text-only Button

This section showcases an example of the `Primary` button component in `iconOnly` state at `text` button type that has a transparent background color and transparent border color that using `.bg-transparent` and `.border-transparent` class from **TailwindCSS**, as specified in `tailwind.config.ts`.

<Canvas of={ComponentStories.IconOnlyTextOnly} />

<Source
  code={`
<Button.Primary iconOnly={true} size="lg" type="text">
  <AiOutlinePlus />
</Button.Primary>
`}
/>
